# Only needed to run the backend outside Docker. `docker compose up` sets these itself.
#
# There is nothing else to put here. The provider, the API key, the model, the spend ceilings and
# the approval gates are all configured on the dashboard's Settings page and stored in the database.

DATABASE_URL=postgresql://sdlc:sdlc@localhost:5433/sdlc
PORT=3001
